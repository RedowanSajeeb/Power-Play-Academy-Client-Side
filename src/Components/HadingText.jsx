import React from 'react';

const HadingText = ({firstText,ShortText}) => {
    return (
        <div>
            <h1>{firstText}</h1>
            <p>{ShortText}</p>
        </div>
    );
};

export default HadingText;